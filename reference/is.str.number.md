# Check if a string is a number

Check if a string is a number

## Usage

``` r
is.str.number(x)
```

## Arguments

- x:

  A string to be checked.

## Value

`TRUE` if the string is a number, `FALSE` otherwise.
