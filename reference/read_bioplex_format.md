# Read the BIOPLEX format data

Read the BIOPLEX format data

## Usage

``` r
read_bioplex_format(path, verbose = TRUE)
```

## Arguments

- path:

  Path to the BIOPLEX file (excel file)

- verbose:

  Print additional information. Default is `TRUE`
