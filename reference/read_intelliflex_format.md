# Read the Intelliflex format data

Read the Intelliflex format data

## Usage

``` r
read_intelliflex_format(path, verbose = TRUE)
```

## Arguments

- path:

  Path to the INTELLIFLEX file

- verbose:

  Print additional information. Default is `TRUE`
